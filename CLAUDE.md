# CLAUDE.md

> **규칙 정본은 [`AGENTS.md`](AGENTS.md) 한 곳뿐이다.** 이 파일은 그걸 불러오고, Claude Code 전용 사항만 덧붙인다.
> **규칙을 고칠 일이 생기면 `AGENTS.md`를 고친다. 여기에 내용을 복사하지 말 것** — 두 파일이 따로 놀면 그때부터 아무도 뭐가 맞는지 모른다.

@AGENTS.md

---

## Claude Code 전용

### 작업 규칙 — 실행 방법

규칙 본문은 **[`AGENTS.md` §8 작업 규칙](AGENTS.md)** 에 있다(위에서 이미 불러왔다). 여기는 그 5개를 **이 저장소에서 실제로 어떻게 지키는지**만 적는다.

| AGENTS.md §8 | Claude Code에서 이렇게 |
|---|---|
| **1. 목적 먼저 확인** | 코드를 건드리기 전에 **한 줄로 "왜"를 말한다.** `AGENTS.md §4` 원칙과 충돌하면 편집하지 말고 **먼저 물어본다** |
| **2. 쓰고 나면 검증** | 아래 **문법 검사 → `shot.py` 캡쳐 → Read로 PNG 열기.** 추천 엔진을 건드렸으면 **4개 날씨 시나리오 전부** |
| **3. 작은 단위로** | Edit 한 번 = 한 가지 변경. 여러 파일을 한꺼번에 갈아엎지 않는다 |
| **4. 에러는 원인부터** | 스택트레이스를 그대로 붙이지 말고 **비개발자가 읽을 한 줄**로 원인을 옮긴 뒤 고친다 |
| **5. 기존 패턴 유지** | **바닐라 HTML/JS 한 장.** npm·프레임워크·빌드 도구 도입 금지. 새 함수를 만들기 전에 `grep`으로 이미 있는지 본다 |

> 🚫 **검증 안 하고 "됐습니다"라고 말하지 않는다.** 이 저장소에서 실제로 그렇게 놓친 버그 목록은 바로 아래에 있다.

### 프로토타입 확인 — 눈으로 봐야 한다

이 저장소의 산출물은 **화면**이다. 코드만 읽고 "됐다"고 하지 말 것. **캐스케이드 충돌·요소 겹침·폰트 폴백은 코드에선 안 보인다.**

```bash
python3 프로토타입/shot.py 오늘 기록          # 화면 캡쳐 → 프로토타입/shots/
python3 프로토타입/shot.py 오늘 --sim lib=thin # 손으로는 못 가는 예외 화면
```

찍은 뒤 **Read 도구로 PNG를 직접 열어 확인**한다. Playwright(1.61 + Chromium)는 이미 설치돼 있다.

> 이 세션에서 **이 방식으로만 잡힌 버그들:** 시뮬레이터 버튼이 탭바를 덮음 · 터치타깃이 CSS 캐스케이드로 64px→52px로 깎임 · 2장만 보여주면서 제목은 "3가지"라고 함 · 사진 24/29 중복.

### 코드 수정 후 필수 검증

```bash
# JS 문법
python3 -c "import re;s=open('프로토타입/rainism_prototype_v1.html',encoding='utf-8').read();open('/tmp/p.js','w').write('\n'.join(re.findall(r'<script>(.*?)</script>',s,re.S)))" && node --check /tmp/p.js
```

```bash
# 배포 환경(http)에서 확인 — file:// 더블클릭에선 안 잡히는 버그가 있다 (AGENTS.md §9-2)
python3 -m http.server 8899   # → http://localhost:8899/rainism_prototype_v1.html
```

**추천 엔진을 건드렸으면 반드시 4개 날씨 시나리오로 하드필터를 돌려본다.** 비 오는 날 비 부적합 코디가 한 장이라도 나오면 **원칙 2 위반이고, 그게 이 제품이 죽는 방식이다.**

**UI 문구를 넣거나 고쳤으면 그 문장이 참인지 코드로 확인한다** (AGENTS.md §9-1). *"올린 사진 분위기를 참고해 골랐어요"* 가 실제로는 거짓이었던 전례가 있다.

### 아티팩트로 공유할 때

프로토타입은 상대경로 이미지(`./proto_img/`)를 쓰므로 **그대로 배포하면 사진이 안 뜬다.** 이미지·폰트를 base64로 인라인한 자립 파일을 만들어 배포해야 한다. (이 세션의 빌드 스크립트 방식 참조 — 사진은 `sips -Z 520`으로 줄여서 2.3MB)

### 말투

사용자는 **비개발자**다. 전문용어를 쓰면 그 자리에서 풀어 쓴다. `AGENTS.md §0` 사전에 없는 말은 특히.

과거에 실제로 있었던 실패: *"스파이크가 뭐고 Phase 0이 뭐고 하드닝이 뭐고 북극성이 뭔데"* — **용어를 던져놓고 설명 안 한 건 내 잘못이다.**
