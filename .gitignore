# 생성물 — shot.py 로 언제든 다시 만들 수 있다
prototype/shots/

# 라이브러리 시드 후보 원본 — 🚫 커밋 금지 (2026-08-08)
# 출처가 정리되지 않은 참고 이미지다. 공개 저장소에 올리면 그대로 인터넷에 뜨고,
# 우리 문서가 "스크래핑 안 함"을 차별점으로 내세우고 있어 앞뒤가 안 맞는다.
# 원본은 각자 컴퓨터에만 둔다. 데모가 쓰는 사진은 prototype/proto_img/ 뿐이다.
seed_photos/

# macOS
.DS_Store

# 파이썬
__pycache__/
*.pyc

# Node / Next.js (지금은 없지만 실서비스 대비 방어적으로)
node_modules/
.next/

# 환경변수·비밀 — 절대 커밋 금지
.env
.env.*

# 핀터레스트에서 받은 핀 기록 — 두 번 받는 걸 막는 용도
# 사진 원본과 마찬가지로 저장소에 올리지 않는다
tech/supabase/imported_pins.txt
tech/supabase/import_*.sql
