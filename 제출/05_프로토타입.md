# rainism — 디자인 프로토타입 & MVP

> 작성 2026-07-13 · 파일: [`프로토타입/rainism_prototype_v1.html`](../프로토타입/rainism_prototype_v1.html)
> **실행: 더블클릭.** 설치·서버·터미널 불필요. 단일 HTML + 실사진 31장.

---

## 1. 이게 뭔가

**목업이 아니라 실제로 작동하는 프로토타입이다.**

- 추천 엔진(**하드필터 → 무드 유사도 → MMR 다양성 → 안전빵 → 이유 템플릿**)이 HTML 안의 JavaScript로 **실제 구현돼 있다.**
- 코디 사진 **31장은 실사진**이며, 사람이 직접 무드·색·TPO·온도밴드·비 적합성으로 태깅했다.
- 날씨를 바꾸면 **추천 결과가 실제로 달라진다.** 화면 전환만 하는 클릭더미가 아니다.

---

## 2. 화면 플로우

```
  [스플래시]
      ↓
  ┌─ 온보딩 (스크리닝) ────────────────────┐
  │  성별 선택 → (남성 선택 시: "준비 중" 안내)
  │  나이대 선택 → (타깃 밖: "기다려 주세요" 안내)
  └────────────────────────────────────────┘
      ↓
  ┌─ 취향 퀴즈 3문항 ──────────────────────┐
  │  Q1 "어떤 게 제일 나 같아요?"     무드
  │  Q2 "손이 자주 가는 색은?"        색
  │  Q3 "요즘 옷 입고 가는 곳은?"     TPO
  └────────────────────────────────────────┘
      ↓
  참고 사진 선택  ← "사진 없이 취향으로 볼래요" 경로 항상 존재
      ↓
  날씨 선택 (데모)  ← 실서비스에서는 기상청 API가 자동
      ↓
  ┌─ 오늘의 3코디 (코어 화면) ─────────────┐
  │  "비 오는 오늘, 입을 만한 3가지"
  │  각 코디 = 사진 + 이름 + 태그 + 한 줄 이유 + 구성 아이템
  │  [다시 추천] 버튼
  └────────────────────────────────────────┘
      ↓ 탭 저장
  ┌─ 하단 3탭 ─────────────────────────────┐
  │  오늘 (코어)  ·  날씨  ·  My(코디 캘린더)
  │  캘린더: 날짜 클릭 → 그날 저장한 코디 + 그날 날씨
  └────────────────────────────────────────┘
```

**폴백 화면도 구현돼 있다** — "오늘은 딱 맞는 코디가 조금 부족해요" (= PRD FR-14 "결과 없음을 보여주지 않는다"의 실물).

---

## 3. PRD와의 대응 — 무엇이 구현됐나

| PRD 요구사항 | 프로토타입 | 상태 |
|---|---|---|
| FR-1 취향 퀴즈 3문항 | Q1 무드 / Q2 색 / Q3 TPO | ✅ |
| FR-3 참고 사진 **선택** ("없이 볼래요" 경로) | 구현 | ✅ |
| FR-4 참고 사진이 **추천의 질의로 쓰임** | **CLIP 임베딩 + 코사인 유사도** (웹 주소로 열 때) | ✅ |
| FR-10 코디 사진 **3장** | 구현 | ✅ |
| FR-11 **하드필터 → 유사도 → 다양성** 순서 | JS로 실제 구현 | ✅ |
| FR-12 한 줄 이유 + 태그 + 구성 아이템 | 구현 | ✅ |
| FR-13 이유 문장 **결정론**(LLM 아님) | 템플릿 | ✅ |
| FR-14 빈손 방지 폴백 | "코디가 조금 부족해요" 화면 | ✅ |
| FR-15 탭 한 번 저장 | 구현 | ✅ |
| FR-18 하단 3탭 (오늘·날씨·My) | 구현 | ✅ |
| FR-19 저장 기록 캘린더 | 날짜별 코디 + 그날 날씨 | ✅ |
| "안전빵 1장" 보장 | 구현 | ✅ |
| 성별·나이 스크리닝 (타깃 니치) | 구현 | ✅ |

---

## 4. 자리표시 — 실서비스에서 교체될 부분 (정직하게)

> 이 3가지는 **프로토타입의 한계이자, 실서비스에서 교체가 이미 설계된 지점**이다.

| 지금 (프로토타입) | 실서비스 | 왜 지금은 이렇게 |
|---|---|---|
| **날씨 = 데모 4시나리오를 직접 선택** | **기상청 API가 자동으로** 오늘 체감온도·강수를 읽음 | 프로토타입에서 여러 날씨를 즉시 비교해 보기 위해 |
| **유사도 = 진짜 CLIP 임베딩** ✅ *(웹 주소로 열었을 때)* | **같은 방식 + pgvector**(서버에서 미리 계산) | 브라우저가 매번 31장을 임베딩하는 건 데모라서 가능한 것 |
| **저장 = 브라우저 메모리** | **Supabase DB (본인만 접근)** | 서버 없이 단일 파일로 돌리기 위해 |

**중요:** 자리표시는 **엔진의 구조가 아니라 데이터 소스**다. 하드필터 → 유사도 → MMR → 안전빵 → 이유의 **순서와 로직 자체는 실서비스와 동일하다.**

### ⚠️ 참고사진 매칭은 "진짜"다 (2026-07-13 추가)

참고 사진 업로드가 **실제로 추천을 바꾼다.** 껍데기가 아니다.

- **CLIP(`Xenova/clip-vit-base-patch32`)이 브라우저 안에서 직접 돈다.** 서버·API 키·비용 0원 (transformers.js).
- 올린 사진 → **512차원 분위기 좌표** → 라이브러리 31장과 **코사인 유사도** → 가까운 순으로 랭킹.
- **검증됨:** 같은 취향·같은 날씨에서 러블리 무드 사진을 올리면 추천 3장이 실제로 러블리·시크 쪽으로 바뀐다.

**단, `file://` 더블클릭에서는 브라우저 보안(CORS) 때문에 모델을 못 불러온다.**
→ 이때는 **자동으로 태그 기반 추천으로 폴백**하고, 화면에 *"오프라인 데모 모드"* 라고 **정직하게 표시**한다.
→ **AI 매칭을 보려면 반드시 웹 주소(Netlify 등)로 열 것.** (§5)

---

## 5. MVP 링크 제출 방법

프로토타입은 **단일 HTML + 이미지 폴더**라 빌드가 필요 없다. 그대로 정적 호스팅에 올리면 링크가 된다.

**가장 빠른 방법 (선택):**
- **Netlify Drop** — [app.netlify.com/drop](https://app.netlify.com/drop) 에 `프로토타입/` 폴더를 **드래그&드롭**. 계정 없이 즉시 URL 생성. *(가장 쉬움)*
- **GitHub Pages** — 레포에 올리고 Settings → Pages 활성화. 무료·영구.
- **Vercel** — 폴더 드래그 배포. 나중에 실제 앱과 같은 스택.

> ⚠️ **제출 시 반드시 함께 적을 것:** "날씨는 데모 4시나리오 선택식이며, 실서비스는 기상청 API 자동 연동" — 안 적으면 심사자가 날씨 자동화가 안 된 것을 **결함**으로 읽는다. 적으면 **의도된 데모 설계**로 읽힌다.

**모바일에서 보기:** 프로토타입은 모바일 화면 비율(`#phone` 프레임)로 설계돼 있다. 데스크톱에서도 폰 프레임 안에 렌더된다.

---

## 6. 함께 있는 파일

| 파일 | 무엇 |
|---|---|
| [`프로토타입/rainism_prototype_v1.html`](../프로토타입/rainism_prototype_v1.html) | **정본 프로토타입** (47KB, 단일 파일) |
| [`프로토타입/proto_img/`](../프로토타입/proto_img/) | 코디 실사진 **31장** (사람이 직접 태깅) |
| [`프로토타입/랜딩페이지_rainism_portfolio.html`](../프로토타입/랜딩페이지_rainism_portfolio.html) | 서비스 소개용 랜딩 페이지 |

---

## 7. 아직 없는 것 (보강하면 좋을 것)

- **디자인 시스템 문서** — 컬러·타이포·컴포넌트 규격. (지금은 HTML 안에 인라인)
- **와이어프레임/IA 문서** — 지금은 프로토타입 자체가 그 역할을 대신하고 있다. 제출용으로는 **스크린샷 5~6장**이면 충분하다.
- **날씨 탭 상세 설계** — PRD 부록 B #9에 미결정으로 표시됨.
