# rainism — 공통 규칙 (모든 AI 도구·사람 공통)

> 이 파일이 **정본**이다. `CLAUDE.md`는 이 파일을 불러오기만 한다 (두 파일이 따로 놀지 않게).
> 최종 갱신 2026-08-17

---

## 0. 먼저 — 쉬운 말 사전

문서에 자주 나오는 말. **모르는 단어가 나오면 여기부터.**

| 말 | 뜻 |
|---|---|
| **스파이크** | 본격적으로 만들기 전에 **"이거 되긴 하냐" 짧게 해보는 실험.** 며칠짜리 |
| **Phase 0/1/2…** | 그냥 **0단계, 1단계, 2단계** |
| **게이트** | **"여기 통과 못 하면 다음 단계 가지 마"** 하는 체크포인트 |
| **하드닝** | **안 깨지게 튼튼히 만드는 단계.** 오류 처리·속도·법 |
| **북극성 지표** | **이거 하나로 성공/실패를 판단**하는 숫자. rainism = **"아침 알림을 보낸 날, 코디를 골랐냐"** *(2026-08-16 교체 — 이전엔 "다음날 아침에 또 열었냐")* |
| **하드필터** | **오늘 날씨에 못 입을 사진을 아예 빼버리는 것.** 점수를 깎는 게 아니라 후보에서 제외 |
| **임베딩** | 사진을 **숫자 수백 개로 바꾼 것.** 숫자가 비슷하면 분위기가 비슷하다고 봄 |
| **안전빵** | 3장 중 **안 튀는 무난한 것 1장.** (유저 화면엔 "무난해요"로 표시) |
| **MMR** | 3장이 **서로 안 겹치게** 뽑는 방법 |
| **콜드스타트** | 사진이 얼마 없어서 **추천이 반복되고 빈약해지는 문제** |
| **D1 리텐션** | 가입한 다음 날 **다시 온 사람 비율.** *(2026-08-16 북극성에서 내려옴 — 계속 재지만 합격/불합격 판정엔 안 쓴다)* |
| **도달률** | 알림을 보낸 것 중 **실제로 폰에 도착한 비율.** 아이폰은 **홈화면에 추가해야만** 도착한다 |
| **오알림률** | **비 온다고 알림을 보냈는데 안 온 비율.** 이게 높으면 유저가 알림을 꺼버리고, 그러면 제품이 끝난다 |

**🚫 위 단어들은 유저 화면에 절대 안 나온다.** 우리끼리 쓰는 말이다. (라벨 규칙 = [07_information_architecture §3](submission/07_information_architecture.md))

---

## 1. 이 저장소는 뭔가

**기획·리서치 워크스페이스 + 작동하는 프로토타입.** 아직 실제 앱 코드는 없다(빌드 도구·의존성 없음).

- **프로토타입 실행 = 그냥 더블클릭.** `prototype/rainism_prototype_v1.html`
- **화면 캡쳐:** `python3 prototype/shot.py 오늘` → `prototype/shots/`에 저장
  - 화면 이름: `시작 동의 무드 사진 날씨선택 오늘 결정 날씨 기록` · `all`
  - 예외 화면: `--sim lib=thin` / `auth=expired` / `geo=deny` / `kma=down` / `ai=block`
- **예외 경로 직접 보기:** 프로토타입 우하단 **🧪 상황** 버튼

> ⚠️ **새로 만들지 말 것.** 파이썬 서버 등 별도 구현을 새로 짜지 않는다. **이 프로토타입을 기준으로 이어서 작업.**

---

## 2. 제품 한 줄

> ### **"날씨에 맞춰 내 취향대로 오늘 입을 코디를 아침마다 골라주는 앱."**

- **날씨**가 진입 트리거 (특히 비·눈 오는 궂은 날)
- **내 취향**이 개인화의 축
- **골라준다** = 예쁜 추천이 아니라 **결정 대행.** 이게 본질

**타깃:** 20대 여성 · 데일리룩 · 여름부터. 페르소나 = **"서연"(22세, 아침 8:10, 옷 결정에 쓸 시간 3~5분)**

**동작:** 사진 12장 중 최대 3장 고르기 → 오늘 날씨 + 취향에 맞는 **코디 사진 3장** → 탭 한 번 결정 → 기록

---

## 3. 어디를 보면 되나 — 문서 지도

### 📌 제출용 정본 = `submission/` (여기부터 본다)

| 파일 | 뭐가 있나 | 언제 보나 |
|---|---|---|
| [`01_service_overview`](submission/01_service_overview.md) | 제품 정의 · 경쟁 대비 · 성공 정의 | **"이게 뭐 하는 앱이지?"** |
| [`02_customer_profile`](submission/02_customer_profile.md) | 페르소나 · VOC · **감정 지도** | **"유저가 왜 이러지?"** |
| [`03_PRD`](submission/03_PRD.md) | **요구사항 정본**(FR/NFR/RG) · 미결정 목록 | **"뭘 만들어야 하지?"** |
| [`04_tech_review`](submission/04_tech_review.md) | 스택 · 비용 · 타당성 | **"어떻게 만들지?"** |
| [`05_prototype`](submission/05_prototype.md) | 프로토타입 설명 | |
| [`06_design_principles`](submission/06_design_principles.md) | **원칙 6개 + 우선순위** | **"이 기능 넣어? 말아?"** ⬅ 다툼이 나면 여기 |
| [`07_information_architecture`](submission/07_information_architecture.md) | 화면 목록 · **라벨 사전** · 위계 | **"이걸 뭐라고 부르지?"** |
| [`08_user_flow`](submission/08_user_flow.md) | Happy Path · **예외 경로** · UI 상태 | **"에러 나면 어떡하지?"** |
| [`09_design_system`](submission/09_design_system.md) | shadcn/ui · 토큰 · 폰트 | **"UI 뭘로 만들지?"** |

### 기술 상세 = `tech/`

| 파일 | 뭐가 있나 |
|---|---|
| [`rainism_mvp_implementation_plan_v2`](tech/rainism_mvp_implementation_plan_v2.md) | **Phase 0~4 · 게이트 · 임계경로** ⬅ 만들 때 이거 보고 |
| [`data_model_v2_mood_matching`](tech/data_model_v2_mood_matching.md) | 저장할 것들 |
| [`library_sourcing_plan_v1`](tech/library_sourcing_plan_v1.md) | 사진 700장 어디서 · 얼마 |
| [`external_dependencies_v1`](tech/external_dependencies_v1.md) · [`dependency_risk_mitigation_v1`](tech/dependency_risk_mitigation_v1.md) | 뭐가 죽으면 어떻게 |
| `rainism_outfit_rules_v2_allseason_women.csv` | **날씨 → 하드필터 규칙.** *(2026-08-16: 여름 4밴드 → **전 계절 8밴드** + 눈·빙판 규칙 신설. 파일명도 `v1_summer` → `v2_allseason`)* |

### 원자료 = `research/`
페르소나 · VOC · 감정지도 · UX리서치. **"왜 그렇게 정했지?"** 싶을 때.

### 🚫 `_보관(구버전)/`
**옷 조합형(피벗 이전) 폐기 문서. 참고 금지.**

---

## 4. 디자인 원칙 6개 — 다툼이 나면 이걸로 판정

> 상세: [`06_design_principles`](submission/06_design_principles.md)

1. **결정을 되돌려주지 않는다** — 3장 고정. 필터·정렬·더보기 없음. **탈출구도 없다** *(2026-08-17: "다른 거 보여줘" 폐기 — 3장 안에서 결정한다)*
2. **날씨는 틀리면 안 되고, 취향은 틀려도 된다** — 날씨=하드필터, 취향=소프트랭킹
3. **보상이 노동보다 먼저 온다** — 온보딩 질문 3개 **+ 내 옷 고르기 한 화면**이 상한(*2026-08-10 개정*). **옷을 등록·촬영시키지 않는다**(이건 그대로). 로그인은 없음(§11.1)
4. **취향은 말이 아니라 사진으로 묻는다** — 그리고 **앱도 취향을 언어로 단언하지 않는다**
5. **못 하는 걸 숨기지 않는다** — "당신의 옷장"이라 하지 않음. 축하·뱃지 없음
6. **기본은 나만보기** — 공유 기능 없음

### 충돌하면 왼쪽이 이긴다

> **날씨 안전 > 결정 대행 > 마찰 없음 > 반복 회피 > 취향 정확도 > 성장(소셜)**

**한 줄로:** **"취향은 틀려도 되고, 날씨는 틀리면 안 된다."**

---

## 5. 하드 제약 — 🚫 다시 제안하지 말 것

리서치로 이미 닫힌 길들. **"개선"이라며 되살리지 않는다.**

| 🚫 금지 | 왜 |
|---|---|
| **핀터레스트 직접 연동** | 공개 API 없음 · 약관 위반 · 스크래핑 = 저작권 침해 |
| **웹/SNS 패션 이미지 스크래핑** | 저작권 + 부정경쟁 + 약관, 3중 리스크 |
| **노출 사진에 AI 생성 이미지** | 분위기 훼손 + "가짜/광고" 신뢰 리스크. **유저에게 보이는 건 실사진만** (AI 생성은 내부 검증 전용) |
| **배경제거 · 의류 세그멘테이션** | 옷을 조각내 합성하지 않는다. 원본 사진 통째 매칭 |
| **옷 조각 태깅 · 조합 엔진 · 궁합표** | 폐기. 대신 **이미지 임베딩 + 분위기 유사도** |
| **요청마다 LLM으로 이유 생성** | 결정론 방식. LLM은 라이브러리 적재 때만 |
| **날씨 다매체 스크래핑** | **기상청 공공 API** 원천 수치만 |
| **소셜 · 피드 · OOTD 공유 · 커머스** | 검증 단계 제외 |
| **옷 등록·촬영시키기** | 경쟁 앱 **1순위 이탈 원인**(*"전체 촬영에만 10시간"*). 이걸 피한 게 이번 모델의 최대 이점 — **여전히 안 한다.** ※ 사진 없이 **목록에서 골라 체크하는 것**은 다른 물건이라 허용(2026-08-10, 아래 ✅ 참고) |

### ✅ 확정된 것 (뒤집으려면 근거 대고 유저 승인)

- **AI = 이미지 임베딩 + 벡터 유사도**로 통째 매칭 *(질의는 **온보딩에서 고른 사진들** — 2026-08-17에 「참고사진 올리기」가 폐기되면서 입력만 바뀌었다. ⚠️ 프로토타입엔 CLIP 실연동이 더 이상 없다)* *(※ 2026-07-13에 "태그로 대체" 안이 나왔으나 700장 계산으로 기각. 태그는 점수 종류가 9개뿐이라 1등만 23장 동점 → 사실상 랜덤)*
- **추천 순서 고정:** ①날씨 하드필터 → ②분위기 유사도 → ③MMR 다양성 → ④결정론 이유
  - ⚠️ **탈출구(리롤)가 없어진 뒤로 ②③이 더 무거워졌다**(2026-08-17). 3장이 곧 전부다.
- **폴백 완화 순서:** 취향 → 체감온도(±1→±2) → **🚫 강수는 절대 완화 불가**
- **2탭: 오늘 · 기록** *(2026-08-17 · 유저 결정 — 이전엔 3탭이었다)* — **날씨 탭을 없애고 오늘 탭 안의 카드로 합쳤다.**
  - 왜: 날씨는 **코디를 고르는 근거**다. 3장을 보다 *"진짜 비 와?"* 싶어 탭을 옮기면 돌아왔을 때 **결정의 흐름이 이미 끊겨 있다.** 근거는 결론과 같은 화면에 있어야 한다.
  - ⚠️ **날씨 카드를 키우지 말 것.** 커져서 코디 첫 장이 화면 밖으로 밀리면 **결정 대행(우선순위 2위)이 깨지고, 그게 이 통합이 실패하는 방식이다.** 지금은 기온 + 4수치 + 시간별 막대 + 한 줄까지다.
  - ※ 2026-07-13의 *"날씨 탭은 날씨만 보여주고 끝낸다"* 를 뒤집은 것이다. 단 **방향이 반대**다 — 그때 기각된 건 *코디를 날씨 탭에* 넣자는 안이었고, 이번은 *날씨를 오늘 탭에* 넣었다. 코어 탭은 여전히 **오늘** 하나다. 상세 = [`07_information_architecture §②`](submission/07_information_architecture.md)
- **날씨:** 기상청 공공 API (단기예보 + ASOS)
  - ✅ **프로토타입에 실연동됨** *(2026-08-16)* — 고른 동네의 격자번호(`dong_grid.js`)로 단기예보를 받는다. 인증키를 감추려고 중계 한 장(`api/weather.js`)을 거친다. **키를 HTML·JS에 절대 넣지 않는다**(공개 저장소 · `tech/weather_api_key_setup.md`).
  - **비 판정은 강수량(PCP)·강수형태(PTY)로 한다. 강수확률(POP)로 하지 않는다** — CSV[5] 매핑 그대로. 확률 30%만 갖고 비로 치면 예보상 비가 한 방울도 없는 맑은 날까지 레인 코디만 남는다. 확률은 우산 안내에 쓴다.
  - **화면은 '체감'이 아니라 '기온'이라고 쓴다** — 체감온도 산출식이 아직 문서에 없다(PRD §8.3 · "임의 식 확정 금지"). 습도 70%↑ 밴드 보정만 적용한다.
  - 못 받아오면 데모 4시나리오로 내려가되 **화면에 "예시 날씨"라고 밝힌다**(§9-1). `file://`(더블클릭)은 중계가 없어 항상 이 경로다.
- **하드필터 규칙표를 전 계절로 넓혔다** *(2026-08-16)* — CSV가 여름 4밴드(28↑/23–27/20–22/17–19)에서 **8밴드**(+12–16/9–11/5–8/4↓)로, 그리고 **`[6]` 강설(눈)·결빙 규칙**이 새로 생겼다. **눈·빙판도 강수와 똑같이 🚫 절대 완화 불가**다(넘어지는 게 젖는 것보다 크게 다친다).
  - ⚠️ **스코프가 넓어진 게 아니다.** 니치는 여전히 **여름부터**(§2) — **라이브러리 사진이 여름 것뿐**이라 겨울 밴드를 켜면 후보가 0장이 된다. 규칙만 미리 적어둔 것이다.
  - ⚠️ **적설(SNO)은 아직 안 받아온다.** `api/weather.js`는 TMP·REH·POP·PCP·PTY·TMN·TMX만 읽는다. `[6]`을 실제로 켜려면 SNO부터 받아야 한다(CSV `[5]`에 "미구현"으로 표시).
  - ※ **번호 `[1]`~`[5]`는 그대로 뒀다.** AGENTS·코드 주석·문서가 `CSV[5]` 식으로 참조하고 있어서, 새 규칙은 **뒤에 `[6]`으로 붙였다.**
- **하루 한 벌** *(2026-07-14)* — 하루에 고르는 코디는 **0벌 아니면 1벌.** 다른 걸 누르면 **교체**(추가 아님), 같은 걸 다시 누르면 취소. **"오늘 뭘 골랐나"의 정본은 캘린더 한 곳뿐**이다(별도 저장 목록을 두지 않는다 — 두면 "찜한 것"과 "입은 것"이 갈라진다). 상세 = [`03_PRD` FR-19](submission/03_PRD.md)
- **반복 방지 텀** *(2026-07-14)* — **고른 코디 7일 · 보여주기만 한 코디 2일**은 추천에서 제외한다. 후보가 3장 밑으로 마르면 **보여준 것 → 고른 것** 순으로 푼다. **🚫 강수·온도는 사다리에 없다**(절대 안 푼다).
  - ※ **7일은 유저가 정한 값, 2일은 에이전트가 정한 값**(근거 없음 — 라이브러리 39장 기준으로 후보가 마르지 않는 선). 라이브러리가 700장이 되면 다시 튜닝할 것.
  - ※ **일수는 실제 시각이 아니라 코디의 대상일로 센다** *(2026-08-07)* — 밤 모드에서 대상일이 내일로 바뀌기 때문.
- **로그인 화면을 두지 않는다** *(2026-08-07)* — 첫 진입에 **익명 계정이 자동 발급**되고 그 계정이 저장·조회의 주인이 된다. 유저는 로그인을 한 번도 안 한다(원칙 3). 그래도 기록은 서버에 남는다. 소셜 계정 연결(카카오 등)은 **MVP 스코프 아웃**. 상세 = [`03_PRD` §11.1](final-submission/03_PRD.md)
  - ※ 한계: 브라우저 저장소가 지워지면 그 계정으로 못 돌아간다. 검증 단계엔 감수하되 **얼마나 나는지 계측**한다.
- **밤에 정한 코디는 아침에 그대로 넘긴다** *(2026-08-07)* — 밤 모드로 "내일 이거 입을래"를 눌러둔 게 있으면 다음 날 아침엔 **새로 안 뽑고 그 코디를 보여준다.** 단 **오늘 날씨 하드필터를 다시 검사**해서, 밤사이 비 예보가 생겼다든지 해서 탈락하면 **알리고 새로 고르게 한다**(날씨 안전 > 결정 대행). 상세 = [`03_PRD` FR-10c](final-submission/03_PRD.md)
- **아침 알림(웹푸시)은 코어다 — 스코프 아웃 아님** *(2026-08-16 승격)* — 이전엔 "네이티브 단계로 미룸"이었다. 뒤집은 이유: **다음 아침에 다시 오게 할 장치가 「홈화면 추가」 하나뿐**이라는 오래된 리스크를 닫을 방법이 이것밖에 없다. 상세 = [`03_PRD` FR-21](submission/03_PRD.md)
  - 🔴 **아이폰에서 웹푸시는 「홈화면에 추가된 상태 + iOS 16.4 이상」에서만 온다.** 그래서 **순서를 뒤집는다: 홈화면 추가 먼저 → 알림 허용 나중.** ✅ **프로토타입 반영 완료** *(2026-08-17)* — '알림 받을래요'는 이제 권한을 바로 묻지 않고 **①홈 화면 추가 안내 → ②알림 켜기** 두 단계 화면으로 간다. 홈 화면에서 열린 상태면 ①은 접힌다. **PWA(`prototype/manifest.webmanifest` + 아이콘 3종)를 붙여서 '홈 화면에 추가'가 실제로 앱처럼 열린다.** ⚠️ `file://`(더블클릭)에선 manifest 가 안 먹는다 — 웹 주소로 열어야 한다.
  - **위젯은 여전히 🚫** — 네이티브 앱이 있어야 한다. 승격된 건 푸시뿐이다.
  - **알림은 아침 1회**(원칙 5의 실무 규칙 그대로). 매일 보내되 **비 오는 날은 문구가 달라진다.**
- **북극성을 "알림 도달일 결정률"로 교체** *(2026-08-16)* — **아침 알림을 보낸 날 중, 코디를 하나 정한 날의 비율.** 이전 북극성(D1 아침 트리거 리텐션)은 **보조 지표로 내린다**(계속 재되 합격 판정엔 안 쓴다).
  - 바꾼 이유: **비는 매일 오지 않는다.** 매일 열리는 것을 전제한 지표는 "궂은 날이 진입 트리거"(§2)인 이 제품의 성패를 못 잰다.
  - 같이 신설: **오알림률**(비 온다고 보냈는데 안 온 비율) = 원칙 2("날씨는 틀리면 안 된다")를 숫자로 재는 자리.
  - ※ **지표 이름·정의는 에이전트가 정한 값이다.** 합격선 숫자와 **오알림 발송 임계값(강수확률 몇 % 이상)은 여전히 비어 있다** — 근거가 없어 지어내지 않는다(§10).
- **🚫 "다른 거 보여줘" 폐기 — 3장 안에서 결정한다** *(2026-08-17 유저 결정 · 2026-08-07의 "대상일 1회"를 대체)* — 탈출구를 1회라도 두면 *"한 번 더 돌리면 나은 게 있으려나"* 를 하게 되고, **그게 이 앱이 없애려던 결정 피로다.**
  - 🔻 **대가:** 3장이 다 별로인 날 유저가 할 수 있는 게 없다. 감수한다 — 대신 **3장의 품질이 곧 제품**이 된다(하드필터·MMR 다양성·안전빵이 이전보다 무거워졌다).
  - ⚠️ **지표 「리롤률」이 사라졌다.** 첫 3장이 실패한 비율을 재던 유일한 눈금이었다. 대체 눈금은 아직 없다 → `03_PRD` FR-14b · 부록 B
  - 🚫 "1회만이라도 되살리자"는 제안을 다시 꺼내지 않는다.
- **색은 묻지도, 점수에 쓰지도 않는다** *(2026-08-17 유저 결정)* — 온보딩에서 색 문항을 뺀 것(2026-08-16)에서 한 발 더 나가, **색을 추천 점수에 넣지 않기로 했다.** 이유: **온보딩에서 많은 걸 시키고 싶지 않다 — 날씨와 취향만 본다.**
  - 데이터의 `color`(웜톤/쿨톤/다크/뉴트럴) 칸은 **남겨둔다**(MMR 다양성 계산에는 이미 쓰이고 있다). 점수에만 안 쓴다.
  - 🚫 "색 조건을 넣자"는 제안을 다시 꺼내지 않는다.
- **취향 질문 = 사진 12장 중 최대 3장 고르기** *(2026-08-17)* — 4장 중 1장만 고르던 것을 바꿨다. **12장 = 컨셉 4종 × 3장**이라, 그 컨셉의 대표 사진 한 장이 마음에 안 든다고 컨셉을 통째로 놓치지 않는다.
  - **상한 3장인 이유:** 컨셉이 4종뿐이라 4개를 다 고르면 **"전부 = 아무거나"**가 되어 취향 신호가 0이 된다. **하한 1장** — 안 고르면 다음으로 못 간다.
  - **점수는 합산하지 않는다.** 고른 것들 중 **제일 잘 맞는 하나**로 준다. 합산하면 많이 고른 사람만 점수가 커져서 순위가 뒤집힌다.
  - ※ **12장·3장은 에이전트가 정한 값**(근거 없음 — 컨셉 4종이라는 사실에서 역산). 라이브러리가 700장이 되고 컨셉이 늘면 다시 잡을 것. 상세 = [`03_PRD` FR-1](submission/03_PRD.md)
- **프로토타입 UI = 분홍 테마 + 로고 스플래시** *(2026-08-17)* — 파랑에서 분홍으로 갈아탔다. 색은 Figma Make 시안에서 그대로 뽑았다(배경 `#fff0f6`→`#f8d7ea` · 포인트 `#ff6b9d` · 글자 `#4a2338`/`#b0688c`).
  - 스플래시의 도트 수달('루비')은 **뺐다.** 대신 로고 아트 한 장(`proto_img/splash.png`)을 띄운다. **2.4초** 동안 뿌옇게 떠오르며 또렷해진다.
  - **토큰 정본 = [`09_design_system §4`](submission/09_design_system.md)** *(2026-08-17 분홍으로 갱신)*. 그 절에 **분홍으로 갈아타며 포기한 것**(사진과 색이 싸운다)과 그걸 막는 규칙 3개도 같이 적어뒀다.
- **내 옷 고르기 = 사진을 흩뿌린 화면** *(2026-08-17 유저 결정)* — 글자 칸도, 3열 카드도 아니고 **배경 위에 옷 사진을 그냥 널어놓는다**(레퍼런스 = `neal.fun/wiki-spy`). 카드·테두리·그룹 제목이 전부 없어져 **옷만 남는다.**
  - **한 카테고리에 사진이 여러 장이다** — 21종 / 사진 39장(`proto_img/item/*.png`, 1.2MB). 같은 종류 아무거나 누르면 그 종류가 켜지고, **같은 종류 사진이 전부 같이 켜진다.**
  - **배치는 난수가 아니라 「격자 + 흔들기」다.** 완전 난수로 뿌리면 겹치거나 한쪽만 비어 오히려 지저분하다. 3열 격자를 만든 뒤 칸 안에서 위치·크기를 흔든다. **흔드는 값은 번호로 만들어서 열 때마다 같은 자리에 나온다**(매번 바뀌면 멀미난다).
  - **고른 표시 = 「고른 것만 밝고 나머지는 어두워진다」** *(2026-08-17 유저 결정)* — 고른 것에 테두리를 두르는 게 아니라 **안 고른 사진들을 어둡게** 한다(밝기 42% · 채도 45%). 여기에 ✓ 와 이름표가 붙는다(색·밝기만으로 구분하지 않는다 — NFR-7).
    - 🚫 **위에 검은 막을 덮는 방식은 버렸다** — 막이 네모난 상자 자국으로 그대로 보였다. **사진 자체를 어둡게** 해야 배경(분홍)이 안 죽는다.
    - 이름표는 **사진 바깥이 아니라 안쪽**에 붙인다. 밖으로 빼면 겹쳐 놓은 옆 사진에 가려진다(쌓임 순서를 못 이긴다).
  - **누르기 전엔 이름을 모른다** — 이름표가 고른 뒤에만 뜬다. 부츠·장화는 봐서 알지만 **가디건/재킷, 블라우스/셔츠**는 사진만으로 구분이 안 된다. 흩뿌리기를 택한 대가다.
  - ⚠️ **터치 영역은 96~137px**(NFR-7 하한 46px 위). 사진 크기를 더 줄이지 말 것.
  - *(이전 단계: 라이브러리 사진을 잘라 누끼)* — 아래 그대로.
  - **만든 방법:** 사진 01~40번을 통째로 누끼(rembg) → **사람 키 기준으로** 그 옷이 있는 높이만 잘라냄(상의 14~53% · 겉옷 21~62% · 하의 36~80% · 신발 80~100%) → 투명 여백 정리. 어느 사진에서 어느 옷을 땄는지는 프로토타입 `ITEM_ART` 주석 옆에.
    - 사람 위치가 사진마다 달라서 **원본 좌표가 아니라 인물 키의 비율로** 잘랐다. 겉옷이 21%부터인 건 셀카가 많아 얼굴이 따라 들어왔기 때문.
  - **새 사진을 들여오지 않았다** — 이미 배포돼 있는 01~40번을 잘라 쓴 것이라 라이선스가 늘지 않는다(RG-4).
  - **§5의 🚫 배경제거·세그멘테이션과 부딪히지 않는다.** 그 금지는 *옷을 조각내 코디를 합성하는 것*이고, 이건 내가 가진 옷을 체크할 때 알아보라고 붙인 아이콘이다. **추천은 지금까지대로 원본 사진 통째 매칭.**
  - ※ **먼저 SVG로 21종을 그렸다가 버렸다**(유저가 사진을 원함). 사진을 밖에서 구하는 길은 다 막혀 있었다 — 위키피디아 대표 사진은 사람이 입고 있는 사진, 웹/SNS 수집과 AI 생성은 🚫, 위키미디어 공용은 21종 중 5~6종만 쓸 만했다. **그래서 바깥에서 구하지 말고 있는 사진을 자르는 게 답이다.**

---

## 6. 라벨 규칙 — 유저 화면에 쓰는 말

> 상세: [`07_information_architecture §3`](submission/07_information_architecture.md)

| 행동 | ✅ 이렇게 | ❌ 이러지 마 |
|---|---|---|
| 코디를 오늘 것으로 정함 | **"오늘 이거 입을래"** → **"오늘 입기로 했어요"** | 저장 · 찜 · 담기 |
| 어제 것 확인 묻기 | **"어제 이거 입었어요?"** *(기록 탭에서만 · 2026-08-17)* | 착용 기록 · 피드백 요청 |
| 실제로 입었는지 | **"입었어요"** | 착용 확인 · 체크인 |
| 3장이 별로 | ~~"다른 거 보여줘"~~ **(2026-08-17 폐기 — 3장 안에서 결정)** | 다른 분위기로 · 리롤 |
| ~~참고 사진~~ | **🚫 2026-08-17 폐기** — 취향은 온보딩에서 고른 사진으로 받는다 | 레퍼런스 업로드 |
| 탭 이름 | **오늘 · 기록** | 홈 · 날씨 · My |

**"입을래 → 입기로 했어요 → 입었어요"** — 같은 흐름은 **시제만** 바뀐다.

**취향을 단언하지 않는다:**
- ✅ "체감 23도, 종일 비 소식이에요" (사실 — 반박 불가)
- ❌ "시크 좋아하시죠" (단언 — *"내가?"* 로 반박당함)

---

## 7. 작업할 때

- 🚨 **짧게 답한다. 핵심만.** *(2026-08-15 추가 — 유저 요청)*
  - **질문 하나에 답 하나.** 묻지 않은 배경·대안·주의사항을 얹지 않는다.
  - 표·소제목으로 답을 부풀리지 않는다. **세 줄로 되는 답은 세 줄로.**
  - 유저가 이미 결론을 정하고 물으면(*"이거 할 거니까 해놔"*) **설득하지 말고 그냥 한다.**
  - 반대 의견은 **한 줄**만. 유저가 다시 말하면 그걸로 끝, 두 번 꺼내지 않는다.
  - 길게 써도 되는 때: **유저가 "자세히" "왜" "설명해줘"라고 했을 때뿐.**
- 사용자는 **비개발자/기획자.** 쉬운 말 + 눈에 보이는 산출물. **코드 설명보다 "왜/무엇을".**
  - 위 §0 사전에 없는 용어를 쓰면 **그 자리에서 풀어 쓸 것.**
- **문서를 고치면 정본도 같이 고친다:** 새 결정이 생기면 `AGENTS.md` → `document_map_v1.md` → 해당 문서 순으로.
- **프로토타입을 고치면 반드시 눈으로 확인한다** (`shot.py` 또는 Playwright). 캐스케이드 충돌·겹침은 코드만 봐선 안 보인다.
- **스스로 판단해서 정한 값은 밝힌다.** (밤 기준 17시, 최근 착용 제외 2일 등은 근거 없이 정한 값)

---

## 8. 작업 규칙 — 코드를 건드릴 때

> 이 저장소의 코드는 **프로토타입 한 파일**(`prototype/rainism_prototype_v1.html`)뿐이다. 아래 5개는 그 파일을 고칠 때 지킨다.
> 실행 명령어는 [`CLAUDE.md`](CLAUDE.md) "Claude Code 전용"에.

**1. 코드를 쓰기 전에 그 단계의 목적을 먼저 확인한다.**
"뭘 고치는가"가 아니라 **"왜 고치는가"**를 먼저 말한다. 목적이 §4 디자인 원칙과 부딪히면 **코드를 쓰기 전에 그것부터 꺼낸다.**

**2. 쓰고 나면 반드시 실행해서 검증한다.**
문법 검사 + **화면 캡쳐**. 이 저장소의 산출물은 화면이라 **코드만 읽어선 안 보이는 버그가 나온다**(캐스케이드 충돌·요소 겹침·폰트 폴백). 추천 엔진을 건드렸으면 **4개 날씨 시나리오를 전부** 돌린다.
🚫 **"고쳤습니다"를 검증 없이 말하지 않는다.**

**3. 한 번에 조금씩 고친다.**
한 번에 여러 군데를 고치면 **뭐가 깨뜨렸는지 못 찾는다.** 한 가지 고치고 → 검증하고 → 다음. 큰 작업이면 쪼개서 유저에게 순서를 먼저 보여준다.

**4. 에러가 나면 원인을 설명한 다음에 고친다.**
증상만 덮지 않는다. **왜 났는지 말하고, 그 다음에 고친다.** 유저는 비개발자다 — 원인을 쉬운 말로 한 줄 설명하는 게 수정 자체보다 중요할 때가 많다.

**5. 새 코드는 기존 패턴에 맞춘다.**
이 파일은 **바닐라 HTML/JS 한 장**이다. 프레임워크·빌드 도구·npm 패키지를 끌어들이지 않는다(§1 "새로 만들지 말 것"). 기존 함수 이름·주석 밀도·한글 주석 스타일을 그대로 따른다. **비슷한 걸 새로 만들기 전에 이미 있는지 먼저 찾는다.**

---

## 9. 🚨 되는 척하지 않는다 — 재발 방지 (2026-07-13 추가)

> 이미 두 번 당했다. 증상은 달랐지만 **뿌리는 하나다: 실제로 되는지 확인하지 않고, 된 것처럼 말했다.**
> 이건 §4 **원칙 5(못 하는 걸 숨기지 않는다)를 유저가 아니라 우리 자신에게** 적용한 것이다.

### 9-1. 화면이 유저에게 거짓말하게 두지 않는다

> **사건:** 참고사진을 올려도 추천에 **아무 영향이 없었는데**, 화면에는 *"📷 올린 사진 분위기를 참고해 골랐어요"* 라고 떴다. 사진을 넣든 안 넣든 **똑같은 3장**이 나왔다. 코드를 뒤지기 전까지 아무도 몰랐다. 심사자가 사진을 올려보고 결과가 그대로인 걸 발견했다면 **그 자리에서 신뢰가 끝났다.**

- 기능이 **실제로 하지 않는 일을 문구로 주장하지 않는다.** *"~해서 골랐어요"* 는 정말 그렇게 골랐을 때만 쓴다.
- **자리표시(껍데기)는 자리표시라고 화면에 밝힌다.** 예: *"오프라인 데모 모드 — AI 매칭이 꺼져 있어요"*
- UI 문구를 새로 넣거나 고칠 때 **그 문장이 참인지 코드로 확인한다.** 참이 아니면 **문장을 바꾸거나, 기능을 만든다.** 둘 중 하나다.
- 데모의 자리표시 목록(날씨 데모 선택 · 저장=메모리 등)은 [`05_prototype`](submission/05_prototype.md)에 유지한다. **목록에 없는 새 껍데기를 만들지 않는다.**

### 9-2. 검증은 "실제로 배포될 환경"에서 한다

> **사건:** `<meta charset>` 이 없어서 **한글이 전부 깨졌다.** 그런데 **더블클릭(`file://`)으로 열면 멀쩡해 보였다** — 크롬이 알아서 인코딩을 맞춰줬기 때문. **웹서버로 띄우자마자 깨졌다.** 그대로 Netlify에 올렸으면 심사자는 깨진 화면을 봤다.

- **더블클릭만 보고 "됐다"고 하지 않는다.** 배포는 `file://` 이 아니라 **웹서버(`http://`)로** 된다.
- 검증은 로컬 웹서버로 한 번 더: `python3 -m http.server 8899` → `http://localhost:8899/rainism_prototype_v1.html`
- ⚠️ **`file://` 과 `http://` 는 동작이 다르다.** 이 프로토타입도 **CLIP 임베딩 매칭이 `http://` 에서만 켜지고**, `file://` 에선 태그 기반으로 폴백한다. **"내 컴퓨터에선 되던데"가 통하지 않는 이유.**
- 배포한 뒤에도 **실제 주소를 직접 열어본다:** 한글 · 사진 · 폰트 · `robots.txt`.

### 9-3. 확인 안 된 건 "확인 안 됐다"고 말한다

- **"됐습니다"는 실행해서 눈으로 본 뒤에만** 쓴다.
- 못 봤으면 이렇게 쓴다: **"코드는 고쳤는데 아직 화면으로 확인 못 했습니다."**
- 유저는 비개발자다. **에이전트가 "됐다"고 하면 그대로 믿는다.** 그 신뢰를 근거 없이 쓰지 않는다.

---

## 10. 지금 상태 (2026-08-17)

- ✅ 프로토타입 작동 — 온보딩 → 3코디 → 결정 → 기록 + **예외 경로 5종**
- ✅ 문서 정본 9종(`submission/`) + 최종본 5종(`final-submission/`) + 구현계획 v2
- ✅ **PRD 정합성 보정 완료** *(2026-08-07 결정 · **실제 문서 반영은 2026-08-08**)* — 본문↔부록 모순 2건 해소(로그인 시점·지표 이름), 부록에만 있던 기능을 FR로 승격(**FR-14b** 다른 거 보여줘 · **FR-20b** 어제 확인 배너 · **FR-10c** 밤→아침 인계), 없던 요구사항 신설(**§11.1** 로그인 없음/익명 계정 · **NFR-7** 접근성 · **RG-6** 삭제). 지표 **저장률 → 결정률·착용률**로 개명(화면 라벨과 어긋나서).
  - ⚠️ **`submission/03_PRD.md`와 `final-submission/03_PRD.md` 양쪽에 같은 문장으로 넣었다.** 두 파일의 §1~15는 같은 본문이라 한쪽만 고치면 갈라진다. **앞으로도 요구사항을 고치면 반드시 두 파일 다.**
  - 🗑️ **같이 정리:** `wireframes/uploads/`에 있던 03_PRD·05_prototype·08_user_flow **옛 사본 3개를 삭제**했다(피그마 업로드용 스냅샷 = 세 번째 사본이라 계속 어긋났다). 내용은 전부 정본에 있다. **문서 사본을 또 만들지 말 것.**
- ✅ **코디 3장 아래 버튼 4개 제거 + 리롤 폐기** *(2026-08-17 유저 결정)* — 위 §4 원칙 1 · §5 참고.
  - 뺀 것: `🔄 다른 거 보여줘`(**기능까지 폐기**) · `📷 이 사진처럼` · `🌤 다른 날씨로 보기(데모)` · `↻ 처음부터`
  - 데모 날씨·초기화는 우하단 **🧪 상황** 패널로 계속 갈 수 있다.
- ✅ **참고사진 기능 폐기 (FR-3·FR-4)** *(2026-08-17 유저 결정)* — 온보딩(2026-08-15)에 이어 결과 화면 입구까지 없앴고, **코드도 통째로 지웠다**(약 110줄).
  - **왜:** 온보딩에서 사진 12장 중 3장을 고르는 것(FR-1)이 이미 **사진으로 취향을 받는 일**을 한다. 또 올리게 하는 건 같은 걸 두 번 시키는 노동이다(원칙 3).
  - **AI 방식은 안 바뀐다.** 질의가 *"올린 사진"* → *"고른 사진들"* 로 바뀐 것뿐. **이미지 임베딩 + 벡터 유사도**는 그대로다(DM2가 원래 말한 "앵커 룩 임베딩의 평균"과 같은 그림).
  - 🔴 **같이 사라진 것: 브라우저 CLIP 실연동.** 프로토타입은 이제 **태그 점수로만** 3장을 고른다. 확정된 AI 방식이 작동한다는 **화면 증거가 없다** → **착수 전 스파이크①의 무게가 커졌다.**
  - 🔻 **잃은 것:** *"이 사진처럼 골라줘"* 라는 강한 개인화 입구. 취향 해상도가 **12장이 덮는 범위**로 제한된다.
  - 유저 업로드 경로는 **기록 탭의 「그날 사진」** 하나만 남는다 — 모더레이션(FR-5)·PIPA(RG-1)는 거기에 계속 걸린다.
- ✅ **「☂ 레인부츠까지 들어간 코디예요」 제거** *(2026-08-17)* — 바로 위 '옷장에서 꺼낼 것'에 레인부츠라고 이미 적혀 있었다. 이 줄은 **없는 걸 더하라고 알려줄 때만**(*"여기에 레인부츠만 신으면 돼요"*) 값이 있다.
- ✅ **오늘 화면 위쪽 비우기 — 첫 코디가 스크롤 없이 보이게** *(2026-08-17 유저 결정)* — 배너 세 장이 맨 위를 덮어 **코디 3장이 화면 밖으로 밀려났었다.**
  - **밤 배너 완전 제거.** 밤이라는 사실은 **제목이 이미 말한다**("내일 아침 입을 만한 3가지") — 같은 말을 두 번 하지 않는다.
  - **제목을 한 줄로.** `비 오는 오늘,⏎입을 만한 3가지` → `비 오는 오늘 입을 만한 3가지`. 두 줄이면 그만큼 사진이 내려간다.
  - **노란 「예시 날씨」 배너 → 날씨 카드 안 작은 회색 칩**(`예시 날씨 · 기상청 못 받아옴`). 🔴 **없애지 않았다** — 없애면 가짜 날씨를 진짜인 척 보여주게 된다(§9-1). 자리만 줄이고 말은 그대로 한다.
  - **「어제 이거 입었어요?」 배너는 제거.** 🔴 착용 확인이 사라진 게 아니다 — **기록 탭**의 *"아직 안 알려준 날이 N일 있어요"* 로 계속 답한다(FR-20b 개정).
  - ⚠️ **착용률 수치가 낮아질 수 있다.** 배너는 유저가 확실히 오는 시점(아침)에 물었고, 기록 탭은 스스로 들어와야 한다. **코디가 나빠서가 아니라 묻는 자리가 바뀌어서다** — 검증 때 이 둘을 섞어 비교하지 말 것.
- ✅ **날씨 탭 → 오늘 탭 통합 (3탭 → 2탭)** *(2026-08-17 · 프로토타입·문서 양쪽 반영 완료)* — 위 §5 참고. 고친 문서: `AGENTS.md` · `document_map_v1.md` · `03_PRD`(양쪽) · `01_service_overview`(양쪽) · `05_prototype`(양쪽) · `00_readme` · `02_customer_profile` · `06_design_principles` · `07_information_architecture` · `tech/구현계획v2`
  - `07_IA §②`에 **왜 옛 결정을 뒤집었는지**와 **방향이 반대라는 점**을 적어뒀다(그때 기각된 건 *코디를 날씨 탭에*, 이번은 *날씨를 오늘 탭에*).
- ✅ **알림 순서 뒤집기 + PWA** *(2026-08-17)* — '알림 받을래요'가 ①홈 화면 추가 → ②알림 켜기 순서로 간다. `prototype/manifest.webmanifest` + 아이콘 3종을 붙여 홈 화면 추가가 실제로 앱처럼 열린다. ⚠️ 서버 웹푸시는 아직 없다(페이지가 열려 있는 동안만 알림).
- ✅ **취향 질문 다중선택 + 분홍 테마** *(2026-08-17 · 프로토타입·문서 양쪽 반영 완료)* — 위 §5 참고.
  - 같이 정리한 오래된 어긋남: **"퀴즈 3문항"이 문서 곳곳에 남아 있었다**(색·TPO 문항은 2026-08-16에 뺐는데 반영이 안 됨). `01_service_overview`·`02_customer_profile`·`03_PRD`·`04_tech_review`·`05_prototype`·`06_design_principles`·`07_information_architecture`·`08_user_flow`·`10_behavior_list`·`tech/구현계획v2` 를 **질문 1개(사진 고르기)** 로 통일했다.
  - 화면의 **데모 고지 2줄을 뺐다**(포트폴리오 데모 · 사진 라이선스). 고지 내용은 `05_prototype` §자리표시 표에 남겼다.
  - ⏳ **아직 안 고친 것:** `research/*`(그때의 판단 기록이라 그대로 둔다)
- 🔴 **착수 전 남은 것 3개** (구현계획 Phase 0)
  1. **스파이크① — 어느 임베딩이 '무드'를 잡나** (제품의 상한이 여기서 결정)
  2. **라이브러리 커버리지** — 몇 장이 필요한가 (~700장 / ~350만원)
  3. **규제 체크** — PIPA · 모더레이션
- ✅ **북극성 교체 + 아침 푸시 승격** *(2026-08-16 결정 · 문서 반영 완료)* — 위 §5 참고. 고친 문서: `AGENTS.md` · `document_map_v1.md` · `03_PRD`(양쪽) · `01_service_overview`(양쪽) · `08_user_flow` · `10_behavior_list` · `tech/rainism_mvp_implementation_plan_v2`
  - ⏳ **아직 안 고친 것:** `research/*`(그때의 판단 기록이라 그대로 둔다) · `02_customer_profile`·`07_information_architecture`의 "D1" 표기(파생 서술 — 다음 정리 때)
- 🔴 **리스크(교체 후):** 이제 제품의 목숨이 **알림 도달**에 달렸다. 아이폰은 **홈화면 추가를 안 하면 알림이 아예 안 온다** — 옛 리스크가 사라진 게 아니라 **더 커졌다.** 도달률을 못 만들면 북극성 자체를 못 잰다 → `03_PRD` FR-21
- 🔴 **유저가 정해야 할 것:** ①**검증 성공/실패 판정선**(규모·기간·결정률/착용률 하한) ②**오알림 발송 임계값** — 강수확률 몇 % 이상에서 "비 온다"고 알릴 것인가. 문서에 근거가 없어 에이전트가 정하면 지어내는 것이 된다 → `03_PRD` §4 · 부록 B
